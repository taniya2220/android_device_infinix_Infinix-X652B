name: Makefile CI

on:
  push:
    branches: [ "full_x652b_h627-user-9-PPR1.180610.011-153024-release-keys" ]
  pull_request:
    branches: [ "full_x652b_h627-user-9-PPR1.180610.011-153024-release-keys" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: configure
      run: ./configure

    - name: Install dependencies
      run: make

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
